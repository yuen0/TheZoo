import React from "react";
import { useState, useEffect } from "react";
import iAnimals from "../models/IAnimals";

export default function AnimalInfo() {
	const renderInfo = <div>Hello</div>;

	return <>{renderInfo}</>;
}
